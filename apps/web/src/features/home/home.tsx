import { Avatar } from '@/components/avatar';
import { Recommendation } from '@/features/app/types';

export function Home({ recommendations, onStartConversation, onDismiss, onBlock }: { recommendations: Recommendation[]; onStartConversation: (userId: string) => Promise<void>; onDismiss: (userId: string) => Promise<void>; onBlock: (userId: string) => Promise<void> }) {
  const person = recommendations[0];
  return <div className="home-page"><section className="home-hero"><div><p className="home-kicker">Мужская опора</p><h2>Разговор может начаться спокойно.</h2><p>Выберите человека, с которым хочется познакомиться, и напишите первым, когда будете готовы.</p></div><div className="hero-visual"><span className="hero-visual-label">Новые знакомства</span><div className="hero-orbit orbit-one" /><div className="hero-orbit orbit-two" /><div className="hero-orbit-main">◡</div></div></section><section className="recommendation-section"><header className="recommendation-heading"><div><p className="home-kicker">Анкета дня</p><h2>Кому можно написать</h2></div>{person && <span>1 из {recommendations.length}</span>}</header>{person ? <article className="recommendation-card"><div className="recommendation-photo">{person.profile?.avatarUrl ? <img src={person.profile.avatarUrl} alt={`Аватар ${person.displayName}`} /> : <Avatar name={person.displayName} />}<span className="person-online">Сейчас в сообществе</span></div><div className="recommendation-content"><div className="recommendation-person"><div><h3>{person.displayName}</h3><p>{person.profile?.city ?? 'Город не указан'}</p></div><span className="person-marker">◌</span></div><p className="recommendation-bio">{person.profile?.bio || 'Пока не рассказал о себе.'}</p>{Boolean(person.profile?.interests.length) && <div className="tags">{person.profile?.interests.slice(0, 5).map((item) => <span key={item}>{item}</span>)}</div>}<div className="recommendation-actions"><button className="recommendation-action skip" aria-label="Пропустить анкету" title="Пропустить" onClick={() => void onDismiss(person.id)}>×</button><button className="recommendation-message" onClick={() => void onStartConversation(person.id)}>Начать диалог <span>↗</span></button></div><div className="recommendation-secondary"><button className="text-button" onClick={() => void onDismiss(person.id)}>Посмотреть следующую</button><button className="text-button danger-link" title="Скроет анкету и запретит сообщения в обе стороны" onClick={() => void onBlock(person.id)}>Скрыть и заблокировать</button></div></div></article> : <div className="empty-recommendations"><h3>Пока новых анкет нет</h3><p>Когда появятся подходящие собеседники, они будут здесь.</p></div>}</section></div>;
}
